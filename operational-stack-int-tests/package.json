{
  "name": "operational-stack-int-tests",
  "version": "1.0.0",
  "description": "Integration tests for deployed Operational Hooks handlers",
  "private": true,
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.632.0"
  }
}
